/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'static.vecteezy.com', 'images.unsplash.com', 'res.cloudinary.com'],
    },
}

module.exports = nextConfig
